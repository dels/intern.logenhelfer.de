<% title t("activerecord.actions.create_new", :model => t("activerecord.models.seeker")) %>

<% if can? :index, Seeker %>
<p class="horizontal-menu"><%= show_link_to_all seekers_path %></p>
<% end %>

<%= render 'form' %>

