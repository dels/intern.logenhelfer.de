<% title t("activerecord.actions.create_new", :model => t("activerecord.models.event")) %>

<% if can? :index, Event %>
<p class="horizontal-menu"><%= show_link_to_all events_path %></p>
<% end %>

<%= render 'form' %>

