<%= form_for @attached_file, :html => { :multipart => true } , builder: DefiniteFormBuilder do |f| %>
  <%= f.error_messages %>
  <dl>
    <%= f.file_field :file %>
    <%= f.hidden_field :directory_id, :value => params[:directory] %>

    <%= f.submit %>
  </dl>
<% end %>
