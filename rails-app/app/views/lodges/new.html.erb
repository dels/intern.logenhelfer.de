<% title t("activerecord.actions.create_new", :model => t("activerecord.models.lodge")) %>

<% if can? :index, Lodge %>
<p class="horizontal-menu"><%= show_link_to_all lodges_path %></p>
<% end %>

<%= render 'form' %>

