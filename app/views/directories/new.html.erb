<% title t("activerecord.actions.create_new", :model => t("activerecord.models.directory")) %>

<% if can? :show, @directory.category %>
<p class="horizontal-menu"><%= show_link_to_all @directory.category %></p>
<% end %>

<%= render 'form' %>

