<% title t("activerecord.actions.create_new", :model => t("activerecord.models.announcement")) %>

<% if can? :index, Announcement %>
<p class="horizontal-menu"><%= show_link_to_all announcements_path %></p>
<% end %>

<%= render 'form' %>

