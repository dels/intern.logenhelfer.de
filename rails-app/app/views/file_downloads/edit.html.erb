<% title t("activerecord.actions.edit", :model => t("activerecord.models.file_download")) %>

<p class="horizontal-menu">
  <%= show_link_to @file_download if can?(:show, @file_download) %>
  <%= show_link_to_all file_downloads_path if can?(:index, FileDownload) %>
</p>

<%= render 'form' %>

