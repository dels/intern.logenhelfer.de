<%= form_for @announcement, builder: DefiniteFormBuilder do |f| %>
  <%= f.error_messages %>
  <dl>
    <%= f.text_field :title %>
    <%= f.text_area :message_body %>
    <%= f.submit %>
  </dl>
<% end %>
