<% title "Übersicht für den #{l @date, format: :long}" %>

<p class="calendar icon"><%= raw t '.ical_export', url: export_ical_path %></p>

<%- @whole_day_events.each do |event| -%>
  <%= render event %>
<%- end -%>

<%- @events.each do |event| -%>
  <%= render event %>
<%- end -%>
