<% title t("activerecord.actions.edit", :model => t("activerecord.models.district")) %>

<p class="horizontal-menu">
  <%= show_link_to @district if can?(:show, @district) %>
  <%= show_link_to_all districts_path if can?(:index, District) %>
</p>

<%= render 'form' %>

