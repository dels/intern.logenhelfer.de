<% title APP_CONFIG[:lodge] %>
<% if user_signed_in? %>
  <p><strong>Willkommen Br. <%= current_user.fullname %></strong></p>

  <p>
  </p>
<% else %>
  <p>Bitte loggen Sie sich ein:</p>
  <%= render 'devise/sessions/form' %>
  <%= render 'devise/shared/links' %>
<% end %>
