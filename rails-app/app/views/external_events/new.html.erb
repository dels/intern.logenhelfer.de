<% title t("activerecord.actions.create_new", :model => t("activerecord.models.external_event")) %>

<% if can? :index, ExternalEvent %>
<p class="horizontal-menu"><%= show_link_to_all external_events_path %></p>
<% end %>

<%= render 'form' %>

