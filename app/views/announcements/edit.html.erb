<% title t("activerecord.actions.edit", :model => t("activerecord.models.announcement")) %>

<p class="horizontal-menu">
  <%= show_link_to @announcement if can?(:show, @announcement) %>
  <%= show_link_to_all announcements_path if can?(:index, Announcement) %>
</p>

<%= render 'form' %>

