<% title "Kontakt aktualisiert" %>

<% if current_google_user %>
<%= render 'google_header' %>

<%- if can? :google_sync, User -%>
  <p><%= link_to t('contact_sync.title'), google_sync_users_path, class: 'disk icon' %></p>
<%- end -%>

<% if @update_res %>
<p>
<%=@user.fullname %> erfolgreich synchronisiert.
</p>

<% if @change_messages.empty? %>
<b>Keine &Auml;nderungen</b>
<% else %>
<b>&Auml;nderungen:</b>
<% @change_messages.each do |msg| %>
<p>
  <%= msg %>
</p>
<% end %>
<% end %>
<% else %>
<%=@user.fullname %> konnte nicht synchronisiert werden.
<% end %>
<% else %>
<%= link_to "Sign in with Google", "/auth/google_oauth2", id: "sign_in" %>
<% end %>
