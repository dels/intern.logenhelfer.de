<% title t("activerecord.actions.create_new", :model => t("activerecord.models.user")) %>

<% if can? :index, User %>
<p class="horizontal-menu"><%= show_link_to_all users_path %></p>
<% end %>

<%= render 'form' %>

