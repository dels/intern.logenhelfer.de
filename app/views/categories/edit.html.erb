<% title t("activerecord.actions.edit", :model => t("activerecord.models.category")) %>

<p class="horizontal-menu">
  <%= show_link_to @category if can?(:show, @category) %>
  <%= show_link_to_all categories_path if can?(:index, Category) %>
</p>

<%= render 'form' %>

