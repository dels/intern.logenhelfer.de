<% title t("activerecord.actions.edit", :model => t("activerecord.models.external_event")) %>

<p class="horizontal-menu">
  <%= show_link_to @external_event if can?(:show, @external_event) %>
  <%= show_link_to_all external_events_path if can?(:index, ExternalEvent) %>
</p>

<%= render 'form' %>

